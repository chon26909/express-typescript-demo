pipline { 
    agent { 
        label  "dev"
    }

    environment { 

    }

    stages {

        stage("Init") { 
            steps { 
                echo "Init"
            }
        }

        stage("Build") {
            steps {
                echo "Build"
            }
        }

        stage("Deploy") { 
            steps {
                echo "Deploy"
            }
        }
    }
}